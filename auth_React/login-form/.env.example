## Example env for the Vite frontend
# Copy this file to `.env` in `auth_React/login-form/` and customize values for your environment.

# If empty, the frontend will use relative paths (recommended for dev proxy):
VITE_API_URL=

# To talk directly to a backend host (cross-origin), set a full URL, e.g.:
# VITE_API_URL=http://localhost:8000
VITE_API_URL=http://localhost:8000
