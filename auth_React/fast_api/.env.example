# Copy to `auth_React/fast_api/.env` and customize.
# Example backend env values (dev):
# JWT_SECRET=change-me-please-very-secret
# DB_URL=sqlite://db.sqlite3
# If you need GitHub Codespaces / preview origins allowed for development, set:
# ALLOW_CODESPACES=true
